{
  "_note": "Cold reactions: any decent model; it gets minimal context, so it's cheap.",
  "_help": "null = use the .env default. Copy to agent.json (git-ignored) to customize; keys may go there as api_key / image_api_key or by env var name via api_key_env.",
  "base_url": null,
  "api_key_env": null,
  "model": null,
  "temperature": 0.7,
  "max_tokens": 3000,
  "max_steps": 4,
  "send_images": false
}
