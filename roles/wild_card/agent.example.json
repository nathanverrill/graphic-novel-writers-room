{
  "_note": "Divergent leaps: a cheap model run hot is fine; randomness comes from the code-drawn cards.",
  "_help": "null = use the .env default. Copy to agent.json (git-ignored) to customize; keys may go there as api_key / image_api_key or by env var name via api_key_env.",
  "base_url": null,
  "api_key_env": null,
  "model": null,
  "temperature": 1.1,
  "max_tokens": 4000,
  "max_steps": 6,
  "send_images": false
}
