{
  "_note": "One image per panel (~5 per page). Cheap image settings are fine for thumbnails, e.g. image_extra {\"quality\": \"low\"} on gpt-image-1.",
  "_help": "null = use the .env default. Copy to agent.json (git-ignored) to customize; keys may go there as api_key / image_api_key or by env var name via api_key_env.",
  "base_url": null,
  "api_key_env": null,
  "model": null,
  "generate_images": true,
  "image_model": null,
  "image_extra": {}
}
