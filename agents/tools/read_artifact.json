{
  "description": "Read one project file, e.g. 'story.md', or (the Script Coordinator only) a library file such as 'library/prosperity/input/bible.md'.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      }
    },
    "required": [
      "name"
    ]
  },
  "_why": "The way an agent reaches anything not already in its prompt. Only the Script Coordinator may open library/ files; everyone else is pointed at characters.md, world.md and story.md, which it made from them."
}
