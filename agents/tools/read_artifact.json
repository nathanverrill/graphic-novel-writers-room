{
  "description": "Read one project file, e.g. 'outline.md', or (the Researcher only) a library file such as 'library/prosperity/input/bible.md'.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      }
    },
    "required": [
      "name"
    ]
  },
  "_why": "The way an agent reaches anything not already in its prompt. Only the Researcher may open library/ files; everyone else is pointed at brief.md."
}
